## Stacks app

## Testing

We need headless chromium to run test:

> `/$ npx playwright install chromium`

on windows it should install it in this dir:
`C:\Users\<username>\AppData\Local\ms-playwright\chromium-<version>\`
